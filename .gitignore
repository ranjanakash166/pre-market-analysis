# dependencies
/node_modules

# next
/.next
/out

# production
/build

# misc
.DS_Store
*.pem

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env — never commit real keys (.env.local, .env.production.local, etc.)
# Only .env.example is tracked as a template.
.env*
!.env.example

# cache
/.cache
node_modules
